{
  "name": "@tokenpanel/api",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --target=bun --outdir dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "clean": "rm -rf dist .turbo node_modules/.tmp"
  },
  "dependencies": {
    "@tokenpanel/contracts": "workspace:*",
    "@tokenpanel/db": "workspace:*",
    "effect": "3.22.0",
    "hono": "^4.7"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
